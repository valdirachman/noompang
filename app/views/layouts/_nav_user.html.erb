<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/home"><img class="navbar-brand" src="/logo_noompang.png"></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= form_tag(users_path, :method => "get", id: "search-form", class: "navbar-form", role: "search") do %>
            <div class="form-group">
              <%= text_field_tag :search, params[:search], placeholder: "Search Users", class: "form-control" %>
            </div>
            <%= button_tag(type: "submit", class: "btn") do %>
                <i class="glyphicon glyphicon-search"></i>
            <% end %>
          <% end %>
        </li>
        <li>
          <%= link_to home_path do %>
            <span class="glyphicon glyphicon-home"></span>
          <% end %>
        </li>
        <li>
          <%= link_to friend_request_path do %>
            <span><span class="glyphicon glyphicon-bell"></span> <% if current_user.pending_invited_by.count > 0 %>(<%= current_user.pending_invited_by.count %>)<% end %></span>
          <% end %>
        </li>
        <li>
          <%= link_to conversations_path do %>
            <span class="glyphicon glyphicon-envelope"></span>
          <% end %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user"></span><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="/user/<%= current_user.username %>">Profile</a></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Edit Profile', edit_profile_path(current_user.profile) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to('Log Out', destroy_user_session_path, :method => :delete) %></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
